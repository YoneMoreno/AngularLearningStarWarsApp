import { SortCharactersPipe } from './sort-characters.pipe';

describe('SortCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
